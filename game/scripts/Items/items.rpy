init python:
    class Items(object):
        def __init__(self, name, ID, value, description, number_owned, expire_days):
            return

        def test_function(self):
            return
